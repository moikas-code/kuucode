#!/bin/bash

# Legacy compatibility script
# This script is deprecated. Use ./scripts/generate-sdks instead.

echo "⚠️  DEPRECATED: ./scripts/stainless is deprecated"
echo "🔄 Redirecting to: ./scripts/generate-sdks"
echo ""

# Get the directory of this script
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Call the new script with all arguments
exec "$SCRIPT_DIR/generate-sdks" "$@"
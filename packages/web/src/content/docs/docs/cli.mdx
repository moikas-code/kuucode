---
title: CLI
description: The kuucode CLI options and commands.
---

Running the kuucode CLI starts it for the current directory.

```bash
kuucode
```

Or you can start it for a specific working directory.

```bash
kuucode /path/to/project
```

---

## Commands

The kuucode CLI also has the following commands.

---

### run

Run kuucode in non-interactive mode by passing a prompt directly.

```bash
kuucode run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "kuucode run"
kuucode run Explain the use of context in Go
```

#### Flags

| Flag         | Short | Description                                |
| ------------ | ----- | ------------------------------------------ |
| `--continue` | `-c`  | Continue the last session                  |
| `--session`  | `-s`  | Session ID to continue                     |
| `--share`    |       | Share the session                          |
| `--model`    | `-m`  | Model to use in the form of provider/model |

---

### auth

Command to manage credentials and login for providers.

```bash
kuucode auth [command]
```

---

#### login

Logs you into a provider and saves them in the credentials file in `~/.local/share/kuucode/auth.json`.

```bash
kuucode auth login
```

When kuucode starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
kuucode auth list
```

Or the short version.

```bash
kuucode auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
kuucode auth logout
```

---

### upgrade

Updates kuucode to the latest version or a specific version.

```bash
kuucode upgrade [target]
```

To upgrade to the latest version.

```bash
kuucode upgrade
```

To upgrade to a specific version.

```bash
kuucode upgrade v0.1.48
```

---

## Flags

The kuucode CLI takes the following flags.

| Flag           | Short | Description          |
| -------------- | ----- | -------------------- |
| `--help`       | `-h`  | Display help         |
| `--version`    |       | Print version number |
| `--print-logs` |       | Print logs to stderr |
| `--prompt`     | `-p`  | Prompt to use        |
| `--model`      | `-m`  | Model to use in the form of provider/model |
| `--mode`       |       | Mode to use          |

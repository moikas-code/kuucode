---
title: Config
description: Using the kuucode JSON config.
---

You can configure kuucode using a JSON config file.

```json title="kuucode.json"
{
  "$schema": "https://kuucode.ai/config.json",
  "theme": "kuucode",
  "model": "anthropic/claude-sonnet-4-20250514",
  "autoupdate": true
}
```

This can be used to configure kuucode globally or for a specific project.

---

### Global

Place your global kuucode config in `~/.config/kuucode/kuucode.json`. You'll want to use the global config for things like themes, providers, or keybinds.

---

### Per project

You can also add a `kuucode.json` in your project. This is useful for configuring providers or modes specific to your project.

When kuucode starts up, it looks for a config file in the current directory or traverse up to the nearest Git directory.

This is also safe to be checked into Git and uses the same schema as the global one.

---

## Schema

The config file has a schema that's defined in [**`kuucode.ai/config.json`**](https://kuucode.ai/config.json).

Your editor should be able to validate and autocomplete based on the schema.

---

### Modes

kuucode comes with two built-in modes: _build_, the default with all tools enabled. And _plan_, restricted mode with file modification tools disabled. You can override these built-in modes or define your own custom modes with the `mode` option.

```json title="kuucode.json"
{
  "$schema": "https://kuucode.ai/config.json",
  "mode": {
    "build": { },
    "plan": { },
    "my-custom-mode": { }
  }
}
```

[Learn more here](/docs/modes).

---

### Models

You can configure the providers and models you want to use in your kuucode config through the `provider` and `model` options.

```json title="kuucode.json"
{
  "$schema": "https://kuucode.ai/config.json",
  "provider": {},
  "model": ""
}
```

You can also configure [local models](/docs/models#local). [Learn more](/docs/models).

---

### Themes

You can configure the theme you want to use in your kuucode config through the `theme` option.

```json title="kuucode.json"
{
  "$schema": "https://kuucode.ai/config.json",
  "theme": ""
}
```

[Learn more here](/docs/themes).

---

### Logging

Logs are written to:

- **macOS/Linux**: `~/.local/share/kuucode/log/`
- **Windows**: `%APPDATA%\kuucode\log\`

You can configure the minimum log level through the `log_level` option.

```json title="kuucode.json"
{
  "$schema": "https://kuucode.ai/config.json",
  "log_level": "INFO"
}
```

With the following options:

| Level   | Description                              |
| ------- | ---------------------------------------- |
| `DEBUG` | All messages including debug information |
| `INFO`  | Informational messages and above         |
| `WARN`  | Warnings and errors only                 |
| `ERROR` | Errors only                              |

The **default** log level is `INFO`. If you are running kuucode locally in development mode it's set to `DEBUG`.

---

### Sharing

You can configure the [share](/docs/share) feature through the `share` option.

```json title="kuucode.json"
{
  "$schema": "https://kuucode.ai/config.json",
  "share": "manual"
}
```

This takes:

- `"manual"` - Allow manual sharing via commands (default)
- `"auto"` - Automatically share new conversations
- `"disabled"` - Disable sharing entirely

By default, sharing is set to manual mode where you need to explicitly share conversations using the `/share` command.

---

### Keybinds

You can customize your keybinds through the `keybinds` option.

```json title="kuucode.json"
{
  "$schema": "https://kuucode.ai/config.json",
  "keybinds": {}
}
```

[Learn more here](/docs/keybinds).

---

### Autoupdate

kuucode will automatically download any new updates when it starts up. You can disable this with the `autoupdate` option.

```json title="kuucode.json"
{
  "$schema": "https://kuucode.ai/config.json",
  "autoupdate": false
}
```

---

### MCP servers

You can configure MCP servers you want to use through the `mcp` option.

```json title="kuucode.json"
{
  "$schema": "https://kuucode.ai/config.json",
  "mcp": {}
}
```

[Learn more here](/docs/mcp-servers).

---

### Instructions

You can configure the instructions for the model you're using through the `instructions` option.

```json title="kuucode.json"
{
  "$schema": "https://kuucode.ai/config.json",
  "instructions": ["CONTRIBUTING.md", "docs/guidelines.md", ".cursor/rules/*.md"]
}
```

This takes an array of paths and glob patterns to instruction files. [Learn more
about rules here](/docs/rules).

---

### Disabled providers

You can disable providers that are loaded automatically through the `disabled_providers` option. This is useful when you want to prevent certain providers from being loaded even if their credentials are available.

```json title="kuucode.json"
{
  "$schema": "https://kuucode.ai/config.json",
  "disabled_providers": ["openai", "gemini"]
}
```

The `disabled_providers` option accepts an array of provider IDs. When a provider is disabled:

- It won't be loaded even if environment variables are set
- It won't be loaded even if API keys are configured through `kuucode auth login`
- The provider's models won't appear in the model selection list

---

## Variables

You can use variable substitution in your config files to reference environment variables and file contents.

---

### Env vars

Use `{env:VARIABLE_NAME}` to substitute environment variables:

```json title="kuucode.json"
{
  "$schema": "https://kuucode.ai/config.json",
  "model": "{env:KUUCODE_MODEL}",
  "provider": {
    "anthropic": {
      "options": {
        "apiKey": "{env:ANTHROPIC_API_KEY}"
      }
    }
  }
}
```

If the environment variable is not set, it will be replaced with an empty string.

---

### Files

Use `{file:path/to/file}` to substitute the contents of a file:

```json title="kuucode.json"
{
  "$schema": "https://kuucode.ai/config.json",
  "instructions": ["{file:./custom-instructions.md}"],
  "provider": {
    "openai": {
      "options": {
        "apiKey": "{file:~/.secrets/openai-key}"
      }
    }
  }
}
```

File paths can be:

- Relative to the config file directory
- Or absolute paths starting with `/` or `~`

These are useful for:

- Keeping sensitive data like API keys in separate files.
- Including large instruction files without cluttering your config.
- Sharing common configuration snippets across multiple config files.
